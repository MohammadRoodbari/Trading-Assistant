
services:
  tradingview-mcp:
    build: .
    image: mim/tradingview-mcp:latest
    container_name: tradingview-mcp
    ports:
      - "9080:8000"
    restart: unless-stopped
    environment:
      # Optional: Add any future API keys or environment variables here
      - PORT=8000
      - HOST=0.0.0.0
